apiVersion: v1
kind: Service
metadata:
  name: student-yeryco6-app-service
spec:
  selector:
    app: student-yeryco6-app
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP